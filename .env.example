# Copy to .env and fill in your real values from MongoDB Atlas:
# Atlas → your cluster → Connect → Drivers → Node.js

# Option A: SRV string (app will resolve hosts using public DNS)
MONGO_URI=mongodb+srv://rydergamer1122:rydergamer1122@pos.pgjj9ee.mongodb.net/pos?retryWrites=true&w=majority

# Option B: If SRV still fails, paste the "standard" connection string here instead:
# MONGO_URI_DIRECT=mongodb://YOUR_USER:YOUR_PASSWORD@ac-72qllin-shard-00-00.pgjj9ee.mongodb.net:27017,.../pos?ssl=true&authSource=admin

SESSION_SECRET=change-this-to-a-long-random-string
PORT=3000

# Optional: DNS used before connecting (default fixes many home routers)
DNS_SERVERS=8.8.8.8,1.1.1.1
